# دليل التشغيل الشامل لتطبيق جمعية نماء الأهلية

## نظرة عامة
تطبيق شامل لإدارة التبرعات والمتبرعين لجمعية نماء الأهلية مع إمكانيات متقدمة لإدارة المهام والتقارير.

## متطلبات النظام

### المتطلبات الأساسية
- **Windows 10/11** أو **Linux** أو **macOS**
- **Node.js** (الإصدار 16 أو أحدث)
- **MySQL Server** (الإصدار 8.0 أو أحدث)
- **XAMPP** (للتشغيل المحلي)

### المتطلبات التقنية
- **الذاكرة**: 4GB RAM على الأقل
- **المساحة**: 2GB مساحة قرص متاحة
- **الاتصال**: اتصال إنترنت لإرسال الرسائل

## تثبيت وإعداد قاعدة البيانات

### 1. تثبيت XAMPP
1. قم بتحميل XAMPP من الموقع الرسمي
2. قم بتثبيت XAMPP وتشغيله
3. شغل خدمة MySQL من لوحة التحكم

### 2. إنشاء قاعدة البيانات
1. افتح متصفح الويب واذهب إلى `http://localhost/phpmyadmin`
2. انقر على "New" لإنشاء قاعدة بيانات جديدة
3. اسم قاعدة البيانات: `namaa_db`
4. انقر على "Create"

### 3. تشغيل سكريبت إنشاء الجداول
1. اذهب إلى `http://localhost/phpmyadmin`
2. اختر قاعدة البيانات `namaa_db`
3. انقر على "SQL" من القائمة العلوية
4. انسخ محتوى ملف `database/schema.sql` والصقه
5. انقر على "Go" لتنفيذ السكريبت

## تثبيت التطبيق

### 1. تحميل المشروع
```bash
# انسخ المشروع إلى مجلد XAMPP
cp -r namaa-app/ /opt/lampp/htdocs/
# أو على Windows
# انسخ مجلد namaa-app إلى C:\xampp\htdocs\
```

### 2. تثبيت التبعيات

#### تثبيت تبعيات الخادم الخلفي
```bash
cd backend
npm install
```

#### تثبيت تبعيات الواجهة الأمامية
```bash
cd ../frontend
npm install
```

### 3. إعداد متغيرات البيئة
1. افتح ملف `backend/.env`
2. قم بتعديل الإعدادات حسب بيئتك:

```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=namaa_db
PORT=5000
JWT_SECRET=your_jwt_secret_key_here
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number
```

### 4. إعداد Twilio (اختياري للرسائل)
1. سجل في [Twilio](https://www.twilio.com)
2. احصل على Account SID و Auth Token
3. اشترِ رقم هاتف من Twilio
4. أدخل البيانات في ملف `.env`

## تشغيل التطبيق

### الطريقة الأولى: التشغيل اليدوي

#### 1. تشغيل الخادم الخلفي
```bash
cd backend
npm start
```

#### 2. تشغيل الواجهة الأمامية
```bash
cd frontend
npm start
```

### الطريقة الثانية: التشغيل التلقائي (Windows)
```bash
# انقر مرتين على ملف start.bat
start.bat
```

### الطريقة الثالثة: التشغيل التلقائي (Linux/macOS)
```bash
chmod +x start.sh
./start.sh
```

## الوصول للتطبيق

1. افتح متصفح الويب
2. اذهب إلى `http://localhost:3000`
3. سجل دخولك باستخدام:
   - **اسم المستخدم**: `admin`
   - **كلمة المرور**: `admin123`

## دليل المستخدم

### 1. المتبرعون
- **عرض المتبرعين**: الصفحة الرئيسية تعرض جميع المتبرعين
- **إضافة متبرع جديد**: انقر على "إضافة متبرع"
- **إرسال تذكيرات**: انقر على أيقونة الرسالة بجانب كل متبرع
- **تصدير البيانات**: انقر على "تصدير Excel"
- **استيراد البيانات**: انقر على "استيراد Excel"

### 2. إدارة المهام
- **عرض المهام**: اذهب إلى "إدارة المهام"
- **إضافة مهمة**: انقر على "إضافة مهمة جديدة"
- **تعديل مهمة**: انقر على "تعديل" بجانب المهمة
- **تغيير الحالة**: انقر على المهمة لتغيير حالتها
- **تصدير المهام**: انقر على "تصدير Excel"

### 3. التقارير والإحصائيات
- **عرض التقارير**: اذهب إلى "التقارير"
- **تغيير الفترة**: اختر الفترة الزمنية المطلوبة
- **الرسوم البيانية**: عرض تفاعلي للبيانات
- **تصدير التقارير**: انقر على "تصدير Excel"
- **طباعة التقرير**: انقر على "طباعة التقرير"
- **طباعة بطاقات الشكر**: انقر على "طباعة بطاقات الشكر"

## الميزات المتقدمة

### 1. نظام الرسائل
- **SMS**: إرسال رسائل نصية للمتبرعين
- **WhatsApp**: إرسال رسائل عبر واتساب
- **رسائل مخصصة**: إمكانية تخصيص نص الرسائل

### 2. نظام التقارير
- **إحصائيات التبرعات**: عدد المتبرعين والمبالغ
- **إحصائيات المهام**: حالة المهام والإنجاز
- **الرسوم البيانية**: Pie Chart و Bar Chart
- **الجداول التفصيلية**: بيانات مفصلة للمتبرعين

### 3. التصدير والطباعة
- **تصدير Excel**: للمتبرعين والمهام والتقارير
- **طباعة التقارير**: مع إمكانية تخصيص المحتوى
- **بطاقات الشكر**: تصميم احترافي للطباعة

## استكشاف الأخطاء

### مشاكل شائعة وحلولها

#### 1. "Error connecting to database"
```
الحل:
1. تأكد من تشغيل MySQL في XAMPP
2. تحقق من صحة بيانات الاتصال في .env
3. أعد تشغيل XAMPP
```

#### 2. "Cannot read properties of undefined"
```
الحل:
1. تأكد من تثبيت جميع التبعيات
2. أعد تحميل الصفحة
3. تحقق من console للأخطاء
```

#### 3. مشاكل في الطباعة
```
الحل:
1. استخدم متصفح Chrome أو Firefox
2. تأكد من تفعيل الجافا سكريبت
3. تحقق من إعدادات الطابعة
```

## الأمان

### 1. تغيير كلمة المرور الافتراضية
```sql
UPDATE users SET password = '$2a$10$newhashedpassword' WHERE username = 'admin';
```

### 2. إنشاء مستخدمين جدد
- استخدم صفحة تسجيل الدخول
- اختر أدوار مختلفة (مدير، موظف)

### 3. النسخ الاحتياطي
- احرص على أخذ نسخ احتياطية دورية لقاعدة البيانات
- احتفظ بنسخة من مجلد المشروع

## الدعم الفني

للحصول على الدعم الفني:
1. تحقق من ملف `README.md`
2. راجع ملف `TROUBLESHOOTING.md`
3. تواصل مع فريق التطوير

## الترقيات المستقبلية

### المرحلة التالية المخططة:
- [ ] تطبيق الهاتف المحمول
- [ ] الذكاء الاصطناعي لتحليل التبرعات
- [ ] نظام التقييم والملاحظات
- [ ] دعم عدة لغات إضافية
- [ ] تكامل مع وسائل التواصل الاجتماعي

## الترخيص

هذا المشروع مطور خصيصاً لجمعية نماء الأهلية
جميع الحقوق محفوظة © 2024

---
**ملاحظة**: هذا التطبيق مصمم للاستخدام الداخلي فقط ويتطلب صيانة دورية لضمان الأداء الأمثل.
